# Browser-reachable Graftcode Gateway WebSocket. Copy the host from Vision / gg output.
# Mapped compose port is 8000; do not guess a different registry or path.
VITE_GRAFT_HOST=ws://localhost:8000/ws
